services:
  - type: web
    name: pcbcr-database
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webapp.app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
# Mon Apr 13 14:36:55 WEDT 2026
